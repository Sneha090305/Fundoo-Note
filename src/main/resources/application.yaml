spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fundoo_notes2
    username: root
    password: "&Andy@1293"


    spring:
      rabbitmq:
        host: localhost
        port: 5672
        username: guest
        password: guest

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080