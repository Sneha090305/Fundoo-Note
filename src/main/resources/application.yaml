spring:
  datasource:
    url: jdbc:mysql://localhost:3306/fundoo_notes2
    username: root
    password: "&Andy@1293"

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8080